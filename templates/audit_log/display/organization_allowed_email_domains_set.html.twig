{% import 'audit_log/macros.html.twig' as auditLog %}

<strong>{{ auditLog.organizationLink(display.organization) }}</strong> now requires member email addresses on {{ display.domains|join(', ') }}<br>
Set by: {{ auditLog.userLink(display.actor.username) }}
